# Test project spine PIXI 6.4.2

## Init
`yarn install && yarn start`

### Start local server
`yarn start`

### Build dev version - source maps
`yarn dev`

### Build prod version - minified
`yarn prod`